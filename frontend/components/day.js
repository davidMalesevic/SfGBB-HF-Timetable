import {
  addDays,
  differenceInDays,
  parseISO,
  format,
  isFriday,
  isSaturday,
} from "date-fns";

function Day({}) {
  return (
    <div>
      <div>Datum</div>
    </div>
  );
}

export default Day;
